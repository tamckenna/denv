# bash_profile file (Runs every login shell)

# Load the default .profile
[[ -s "$HOME/.profile" ]] && source "$HOME/.profile"

# Load the .bashrc file
[[ -s "$HOME/.bashrc" ]] && source "$HOME/.bashrc"
